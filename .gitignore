# Secrets — never commit
.env

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# SQLite database — mounted as a Docker volume in production
# Uncomment below if you want to exclude DB from git too (reset on each deploy)
# chf_archive.db

# Editor
.vscode/
*.swp
